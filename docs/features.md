# Features

This page is the single place where feature tags are defined. The tags you see on examples and guide pages link back here so the terminology stays consistent across the docs.

## How To Use These Tags

- examples use tags to show what a project proves in code
- guide pages use the same tags to point back to representative examples
- the tags are descriptive, not marketing labels; they should map to real engine capabilities

## Feature Definitions

{{ feature_catalog }}
